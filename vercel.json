{
  "buildCommand": "pnpm --filter @workspace/efootball-league run build && cp -r artifacts/efootball-league/dist ./dist",
  "outputDirectory": "dist",
  "installCommand": "pnpm install --frozen-lockfile",
  "framework": null,
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
